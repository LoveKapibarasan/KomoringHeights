# KomoringHeights プロジェクト基本原則

## 生成・探索アルゴリズムの設計原則

### 禁止禁止の原則
生成・探索系のコードでは「禁止（skip/continue で候補を完全排除）」を避け、**確率を下げる**方向で実装すること。

- **NG**: `if (bad_condition) continue;` — 完全禁止
- **OK**: `if (bad_condition && rng() % 5 != 0) continue;` — 確率 20% に削減

**理由**: ハードな禁止は分布をいびつにし、想定外の偏りが生じる。人間の判断と同様に「確率を下げる」ことで自然な探索分布を維持する。

**適用例**:
- 追い詰め候補（攻め方に打ち手ゼロ）: 禁止ではなく 20% の確率で採用
- 変同ありの近傍: 禁止ではなく摂動で確率を下げて再試行
- 重複局面: 完全スキップは OK（実際に無意味な重複）
